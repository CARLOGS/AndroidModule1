<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidModule1"
        tools:targetApi="31">
        <activity
            android:name=".ejercise.home.carlogarcia.CarloGarciaMainActivity"
            android:exported="false" />
        <activity
            android:name=".ejercise.home.TeamActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ejercise.classroom.explicitintent.SecondActivity"
            android:exported="false" />
        <activity
            android:name=".ejercise.classroom.explicitintent.HomeActivity"
            android:exported="false"></activity>
        <activity
            android:name=".ejercise.classroom.ImplicitIntentActivity"
            android:exported="true">

            <!-- Hacer public el Intent para aparecer en el Intent Chooser de otra App -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <data android:mimeType="plaintext" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".ejercise.classroom.ExampleManualCreationActivity" />
        <activity
            android:name=".ejercise.classroom.ExampleCreationActivity"
            android:exported="false" />
        <activity
            android:name=".ejercise.classroom.LifeCicleActivity"
            android:exported="false" />
    </application>

</manifest>